# Habit Tracker

A simple habit tracking app built with React and Vite.

## Features
- Add and delete habits
- Track daily completions
- View streaks
- Navigate weeks

## Getting Started
1. Clone the repo
2. Run `npm install`
3. Run `npm run dev` to start development server
4. Run `npm run build` to build for production
